enum bool_enum :=
  .True,
  .False,

let bool_negate b :=
  match b with
    .True => .False,
    .False => .True,
  end

let rec up_to_zero n :=
  if n > 0
  then up_to_zero (n - 1)
  else true

let pair fst snd := fun pair -> pair fst snd

let first x y := x

let main _ := first "hello"
